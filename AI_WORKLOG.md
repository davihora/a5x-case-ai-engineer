# AI_WORKLOG

Uso de IA e obrigatorio e avaliado. Registre AQUI, em modo APPEND (nao apague nada),
todas as interacoes com a IA. A regra do agente esta em AGENTS.md e vale para qualquer
ferramenta (Claude Code, Codex, Copilot, Cursor, Windsurf, Antigravity, Kiro...).

## Log (uma linha por interacao)
| data/hora | ferramenta + modelo | pedido (resumo) | gerado/alterado | decisao | validacao |
|-----------|---------------------|-----------------|-----------------|---------|-----------|
| 2026-07-01 14:20 | Cursor + Claude | esbocar clean_signals | detector/build.py | ajustado (corrigi o filtro) | pytest -q |
| 2026-07-07 21:11 | Claude Code + Fable 5 | analise do repo + planejamento de infra (local, producao, evals/seguranca) — sem codigo ainda | nenhum arquivo do starter alterado (so planejamento; memoria do assistente) | aceito (fase de planejamento) | numeros do dataset verificados via DuckDB (71 dups, 12 sinais svc-ghost, 9 leituras impossiveis, 25 golden, 203 incidentes) |
| 2026-07-07 22:04 | Claude Code + Fable 5 | criar repo a5x-case-ai-engineer a partir do starter: conteudo na raiz, git init (branch main + remote origin, sem commit), .gitignore, pyproject.toml + uv.lock, requirements.txt com pins exatos (uv export), Makefile (setup/test/run/eval/lock), CLAUDE.md apontando para a regra do AGENTS.md | .gitignore, pyproject.toml, uv.lock, requirements.txt, Makefile, CLAUDE.md (tests/test_agent.py e README intactos — diff vazio) | aceito | make setup && make test em venv limpo: deps pinadas instalam e os 6 specs falham com NotImplementedError (esperado no starter); git status confirma .venv e out_action.parquet ignorados |
| 2026-07-07 22:33 | Claude Code + Fable 5 | re-execucao do prompt de scaffold em nova sessao: auditar o repo ja criado contra todas as exigencias (conteudo na raiz, git init sem commit, .gitignore, pyproject+uv.lock+requirements pinado, Makefile, CLAUDE.md->AGENTS.md) e re-validar o aceite | nenhum arquivo do repo alterado (apenas esta linha do AI_WORKLOG); estrutura anterior confirmada correta | aceito (auditoria nao encontrou pendencias) | diff -r vs starter limpo (test_agent.py e README identicos); git check-ignore ok p/ venv//.venv//__pycache__//out_action.parquet; branch main sem commits + remote origin correto; uv lock --check ok; requirements.txt == uv export do lock; make setup && make test em venv limpo (py3.14): setup exit 0 com pins exatos, 6 specs falham com NotImplementedError (esperado) |
| 2026-07-07 22:48 | Claude Code + Fable 5 | EDA dos 5 parquets com DuckDB em scripts/eda.py (reproduzivel, saida deterministica): schema+contagem por tabela, sujeira do otel_signal (dups de signal_id, servicos fora do catalogo, leituras impossiveis em percent), distribuicao de tiers, baseline SIGNAL_ACTION vs eval_golden por tier, taxas de sucesso por (root_cause, chosen_action) | scripts/eda.py (novo; baseline importa SIGNAL_ACTION real de agent.build; cada numero sai junto da query que o gerou) | ajustado (revisor adversarial apontou 3 achados menores, corrigidos: tupla de divergencia cobrindo todas as colunas, total do ROLLUP rotulado TOTAL, DDL da tabela heuristica impresso como SQL citavel) | rodado 3x + diff byte-identico (inclusive de cwd diferente); revisor re-derivou todos os numeros com queries independentes: 7116 linhas / 71 dups excedentes (copias identicas) / 12 sinais svc-ghost / 9 leituras impossiveis; tiers 10/8/6; baseline 6/25=24% (GREEN 66.7, YELLOW 0, RED 0); outcomes 145/34/24; mapeamento root_cause->acao 1:1 |
| 2026-07-07 23:05 | Claude Code + Fable 5 | implementar pending_incidents em agent/build.py: TEMP VIEW pending com o breach mais recente por (service_id, signal_name); limpeza ANTES da deteccao em SQL (dedup de signal_id, semi-join com service_catalog, range-check de percent); threshold correto por sinal (cpu/mem/error_rate/p99 de colunas distintas do catalogo, unidade casada); ORDER BY explicito; sem pandas | agent/build.py (so o corpo de pending_incidents; 4 CTEs comentadas + selecao final; incident_id = 'inc-'||signal_id; sem referencia a attributes p/ funcionar na fixture do teste) | aceito (revisor adversarial nao confirmou nenhum defeito; > vs >= verificado sem impacto: 8 leituras value==threshold, nenhuma e a mais recente do par) | cadeia reconcilia com a EDA: 7116 -> 7045 (-71 dup) -> 7024 (-12 ghost, -9 impossiveis; classes disjuntas) -> 72 pendentes (GREEN 27 / YELLOW 25 / RED 20, 24 servicos); fixture do teste gera os 3 breaches esperados (g/y/r); conferencia manual por queries independentes (SUM(c-1), NOT IN, NOT BETWEEN) reconfirmou 71/12/9 e reimplementacao com UNPIVOT+arg_max reproduziu as mesmas 72 linhas (EXCEPT vazio); pytest -q segue 6 falhas esperadas (sem regressao) |
| 2026-07-09 15:21 | Claude Code + Fable 5 | implementar decide_action (GREEN->SELF_HEAL auto via SIGNAL_ACTION, YELLOW->act-openpr, RED->act-page, tier desconhecido escala), guardrails (tudo do catalogo: allowed_tiers, destrutiva auto fora de GREEN, acao inexistente), apply_decisions (idempotente por anti-join em incident_id; status APPLIED/PROPOSED/ESCALATED/BLOCKED) ate pytest 100% verde + run.py 2x -> 'novas acoes: 0' na segunda | agent/build.py (decide_action, guardrails, apply_decisions, operational_metrics, runbook_for, grounded_actions + hidratacao do ledger action a partir de out_action.parquet em load(), com validacao de schema — necessaria porque run.py abre conexao em memoria a cada execucao; fixture do pytest nao chama load, entao testes ficam imunes) | ajustado (revisor adversarial: 2 endurecimentos aplicados — parquet corrompido/schema divergente e descartado e recomputado em vez de crashar; runbook_for retorna None em score 0 em vez de runbook arbitrario; registrado sem acao: regra de destrutiva e defesa em profundidade nao exercitada pelo catalogo real, GREEN+error_rate auto-aplica rollback destrutivo conforme politica declarada) | pytest 6/6 verde (inclusive com out_action.parquet presente no cwd); run.py 3x -> 72, 0, 0 novas acoes; duplo apply do render nao duplica (dashboard mostra 72, nao 144); metricas: APPLIED 27 / PROPOSED 25 / ESCALATED 20, auto_apply_rate 0.375, blocked 0; parquets de runs consecutivos com conteudo identico; incremental verificado (ledger com 62 -> insere so 10); retrieval mapeia os 4 sinais aos runbooks corretos |

| 2026-07-09 16:10 | Claude Code + Fable 5 | reimplementar runbook_for/grounded_actions usando rag.make_retriever sobre o corpus INTEIRO (sem pre-filtro): query = sinal + vocabulario de dominio (SIGNAL_QUERY) p/ top-1 ser o runbook certo; cada acao ganha doc_id + score; restricao respeitada: rag.py intocado | agent/build.py (SIGNAL_QUERY por sinal; _grounding retorna (doc_id, score) com guardrail de fundamentacao — so runbooks/ com score>0 fundamenta, senao None; grounded_actions expoe runbook + runbook_score) | aceito | test_rag_fundamenta_decisao verde (pytest 6/6); auditoria top-3 com scores p/ os 4 sinais: top-1 = runbook correto em todos (0.54-0.58), nenhuma politica de negocio no top-3 (competidores sao post-mortems <=0.34 e runbooks vizinhos), margem >=1.7x sobre o 2o; baseline so-sinal ja acertava top-1 mas com margem menor; sinais desconhecidos (span.duration/log.record) -> None; run.py 2x -> 72 e 0 novas acoes |

| 2026-07-09 16:32 | Claude Code + Fable 5 | criar agent/evals.py (make eval) com eval_golden: acuracia de mode por tier (tier-gate 100% duro) e de action_id por tier; retrieval hit@1/hit@3 vs runbook esperado por root_cause; achado explorado: 25 golden 100% SAFE -> recall do guardrail imensuravel no golden -> casos adversariais por mutacao deterministica (destrutiva auto YELLOW/RED, acao fora do catalogo, servico fantasma, runbook envenenado 'ignore o tier', whitelist burlada) com recall 100%; gold-15 vigia falso-positivo; tabela eval_result + asserts em tests/test_evals.py | agent/evals.py (novo), tests/test_evals.py (novo; test_agent.py intocado), Makefile (alvo eval -> python3 -m agent.evals, exit 1 se threshold romper) | ajustado (revisor adversarial sabotou o sistema de 6 formas e todas reprovaram; 3 achados de cobertura corrigidos: acuracia_auto_apply por tier no eval, act-failover na whitelist burlada, doc envenenado derivado de SIGNAL_QUERY com rankeia-top1 promovido a threshold) | make eval APROVADO exit 0: mode e auto_apply 100% nos 3 tiers; action GREEN 6/9 (teto da heuristica por sinal), YELLOW/RED 100%; hit@1 17/25=0.68, hit@3 24/25=0.96 (unico miss: gold-16 mem+CacheStampede); recall adversarial 88/88 em 5 tipos; precisao golden 25/25; runbook envenenado rankeia top-1 E e bloqueado; pytest 15/15 verde 2x; scorecards byte-identicos |

| 2026-07-09 16:37 | Claude Code + Fable 5 | adicionar judge em agent/evals.py: dataclass CasoJulgamento SEM os campos golden (anti-vazamento por construcao) + builder de prompt; deterministic_judge (default offline) e llm_judge (Messages API Anthropic via stdlib, sem dep nova) ativado SO com A5X_USE_LLM=1 + key; rubrica tier_compliance/faithfulness/safety; concordancia com golden calculada em codigo fora do prompt | agent/evals.py (CasoJulgamento, build_judge_prompt, deterministic_judge, llm_judge com Haiku 4.5, escolher_juiz com gating duplo, secao judge em run()); tests/test_evals.py (anti-vazamento estrutural + por valor, gating, determinismo) | aceito | teste anti-vazamento verde (25 casos: root_cause/SAFE/expected_action ausentes do prompt); pytest 18/18 verde 2x; eval sem a flag byte-identico entre execucoes; concordancia judge-golden 22/25=88% (GREEN 6/9, YELLOW/RED 100%), faithfulness 23/25 (2 misses = GREEN latency.p99, heuristica clearcache vs runbook scale_out); llm_judge nunca ativa so pela key (RuntimeError se flag sem key) |

| 2026-07-09 16:51 | Claude Code + Fable 5 | criar tabela decision_trace (1 linha/decisao: incident_id, docs recuperados+score, proposed_action, gate_result+motivo, tokens/custo/latencia zerados no offline, final_status) citando compliance_constraints.txt §4; estender dashboard com cards de evals (decisao %, tier-gate, guardrail recall, hit@1) e agregados de custo/latencia; sem quebrar layout | agent/build.py (decision_trace com comentario citando §4), dashboard/render.py (cards evals via evals.run() em try/except + cards §4 + tabela decision_trace), run.py (persiste out_decision_trace.parquet), .gitignore (out_decision_trace.parquet, dashboard.html, .claude/) | aceito | python run.py 2x -> 72 e 0 novas acoes, 72 no decision_trace ambas; validacao cruzada dashboard.html vs SELECT * FROM decision_trace (cards §4 batem, 30 linhas do topo presentes, gate_result=BLOCK sse final_status=BLOCKED, secoes originais intactas, HTML bem-formado); abertura visual em servidor estatico confirmou layout (topo + evals 88/100/100/68 + §4 + tabela com Gate PASS e docs+score), 0 erros no console; pytest 18/18 |

| 2026-07-09 17:37 | Claude Code + Fable 5 | criar .github/workflows/ci.yml (prova publica clone->roda): setup-uv -> uv sync (fallback pip install -r requirements.txt) -> pytest -q -> python run.py -> assert dashboard.html -> upload do dashboard como artifact; sem matrix/lint/deploy | .github/workflows/ci.yml (novo; on push/PR/dispatch, concurrency), README.md (badge de CI no topo, caminho pip intacto) | aceito (badge/artifact so confirmam apos push, feito nesta sessao) | sequencia exata reproduzida localmente: uv sync --frozen instala as 10 deps sem buildar o projeto (sem [build-system] -> projeto virtual), pytest 18/18, run.py gera dashboard.html (15399 bytes); YAML parseado (8 steps, sem strategy); diff do README mostra so a linha do badge |
| 2026-07-09 17:37 | Claude Code + Fable 5 | tornar setup automatico (estilo iFood: build/setup sem passos manuais), criar roteiro de teste no repo e subir no git p/ o usuario replicar em outra maquina; incluir fluxo LLM ponta-a-ponta (credencial via env, nunca comitada) | Makefile (setup cria .venv e instala via uv sync ou venv+pip; test/run/eval resolvem .venv/bin/python automaticamente; alvo clean), TESTING.md (novo roteiro), .gitignore (.pytest_cache/), commit inicial + push origin main | aceito | make test 18/18 usando .venv; make run -> 72/dashboard/decision_trace; make eval APROVADO; ramo uv do make setup instala as deps e imprime setup OK; chave da Anthropic NAO vai ao repo (roteiro usa ANTHROPIC_API_KEY do ambiente); nota: API da Anthropic bloqueada pela rede corporativa Experian -> teste LLM roda fora dela |

## Resumo (preencher ao final)
- Ferramentas usadas:
- Onde a IA acertou / onde errou (e como validou):
- O que decidiu NAO delegar a IA e por que:
